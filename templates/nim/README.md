# Nim Starter Project Template

This is a Nim / ULI-78 starter template. To build wasm binary and import it into
the 'cart.uli' file, ensure you have a Nim compiler, Wasi-SDK and Tic-80 on your system and run:

```
nimble buildcart
```

Run `nimble tasks` command to see more build options.
